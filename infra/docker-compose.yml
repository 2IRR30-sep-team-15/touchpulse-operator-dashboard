name: touchpulse-dashboard
services:
  redis:
    image: redis/redis-stack:latest # includes Redis Search for Redis OM to work
    restart: always
    volumes:
      - db_value:/data
    environment:
      - REDIS_ARGS=--requirepass yourpassword

  backend:
    build: ../backend
    restart: always
    depends_on:
      - redis
    env_file:
      - .env

  frontend:
    build: ../frontend
    restart: always
    depends_on:
      - backend
    environment:
      - NEXT_PUBLIC_BASE_BACKEND_URL='http://localhost'

  nginx:
    image: nginx:1.19.3
    restart: always
    ports:
      - 80:80
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend

volumes:
  db_value:
